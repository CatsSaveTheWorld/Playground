# Keep Django and local IoTCore processes on the anonymous loopback listener.
per_listener_settings true

listener 1883 127.0.0.1
allow_anonymous true

# Raspberry Pi agents connect over the LAN with username/password authentication.
listener 1884 0.0.0.0
allow_anonymous false
password_file /etc/mosquitto/iotcore-agent-passwords
