[Unit]
# Only services that are not already enabled with WantedBy=apache2.service
# belong here. The IoTCore listener, scheduler, and worker are linked by their
# own unit files.
Wants=mosquitto.service zigbee2mqtt.service
