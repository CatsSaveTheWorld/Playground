# Automation time-condition UI v7

- No model or database migration changes.
- Fixes the shared form-shell CSS so radio inputs are not treated as full-width text inputs.
- Renders `지정 시각 / 시간대` as a compact two-option segmented selector.
- Renders weekday checkboxes horizontally with wrapping instead of one weekday per row.
- Schedule behavior and v6 evaluation logic are unchanged.
